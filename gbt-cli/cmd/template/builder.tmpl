//go:build gbt

{{/* DON'T REMOVE go:build directive */}}
{{/* CQC stands for Code Quality Checker, it has many builtin methods. In most cases you can use directly. */}}
{{/* You can do anothing you like in the main method. */}}

package main

import "github.com/kcmvp/gbt/script"

func main() {
	cqc := script.NewCQC()
	cqc.Clean().Test().Build()
}
